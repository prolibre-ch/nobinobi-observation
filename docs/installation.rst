============
Installation
============

At the command line::

    $ easy_install nobinobi-observation

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nobinobi-observation
    $ pip install nobinobi-observation
